<div class="page-lead">
  <div class="page-heading">

  </div>
  <div class="page-actions"></div>
</div>

<%= render "layouts/alerts" %>

<div class="page-main">
  <div class="page-content container">
    <% if current_user.present? %>
      <%= render "surveys/#{@survey}/take" %>
    <% else %>
      <%= render "surveys/#{@survey}/login" %>
    <% end %>
  </div>
</div>
